import React, { useReducer } from 'react';
import HomeLayout from '../components/Home/HomeLayout';

const Home = () => {
  return (
    <div>
      <HomeLayout />
    </div>
  );
};

export default Home;
